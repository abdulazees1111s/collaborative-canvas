# Multi-User Drawing Application

A real-time collaborative drawing canvas built with:
- HTML Canvas API
- Node.js
- Socket.io (WebSockets)

## Features
- Multi-user real-time drawing
- Global undo / redo
- Shared canvas state
- Raw Canvas API (no libraries)

## Run Locally
npm install
npm start

Open http://localhost:3000 in multiple browsers.
